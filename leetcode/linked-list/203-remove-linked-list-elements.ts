import { ListNode } from '../data-structures/list.js'

function removeElements(head: ListNode | null, val: number): ListNode | null {
    const dummy = new ListNode(0, head);

    let prev = dummy;
    let current = head;

    while(current){
        if(current.value === val){
            
        }
    }



};